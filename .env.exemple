VITE_ENABLE_VOICE_CHAT=true

# Live GitHub games: on in dev (Vite proxy). Prod: CI deploys workers/github-proxy when
# CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID secrets are set (see workers/github-proxy/README.md).
# Force off: VITE_ENABLE_LIVE_GAMES=false
